rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WARNING: Development mode only - allows all access
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
